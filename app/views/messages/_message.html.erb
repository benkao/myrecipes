<div class = "well message">

  <p>
    <%= gravatar_for(message.chef, size: 50) %>
    <strong><%= message.chef.chefname %>:</strong>
    <!-- because of naming convention we can refer each message in this message partial without @" -->
    <span class = "content">
      <%= message.content %>
    </span>
  </p>

  <p>
    <small><em>Created <%= time_ago_in_words(message.created_at) %> ago </em></small>
  </p>

</div>